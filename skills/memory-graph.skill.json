{
  "name": "/memory-graph",
  "description": "Carica la memoria del progetto e la fissa in .claude/rules/project-graph.md (auto-caricato ad ogni nuova chat). Se mancano file .md, richiama /organization-graph.",
  "arguments": [
    {
      "name": "model",
      "type": "string",
      "description": "Modello AI che deve usare la memoria"
    }
  ],
  "run": {
    "command": "mem-graph.cmd",
    "args": ["${model}"]
  }
}
