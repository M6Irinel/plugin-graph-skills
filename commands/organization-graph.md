---
description: Genera struttura del progetto: indicizza file, funzioni e componenti in file .md
argument-hint: [folder1] [folder2] ...
allowed-tools: Bash
---

Run `powershell -ExecutionPolicy Bypass -File "${CLAUDE_PLUGIN_ROOT}/commands/organization-graph.ps1" $ARGUMENTS` in the project root (pass one or more folder paths as args) and report the result.
